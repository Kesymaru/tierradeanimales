import { CollectionsConfiguration } from "../models/config";

export const CollectionsConfig: CollectionsConfiguration = {
  case: "case",
  fosterHome: "fosterHome",
  newsletter: "newsletter",
  user: "user",
};

export default CollectionsConfig;
